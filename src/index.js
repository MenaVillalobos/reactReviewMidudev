import React from "react";
import ReactDOM from "react-dom";

const rootElement = document.getElementById("root");

const App = (props) => {
    return <h1>Setting the same as in the example</h1>
}

ReactDOM.render(
    <App/>,
    rootElement
)